import QtQuick 2.5
import "keyboard"

NumericKeyPad {
    id: stockNumericKeyPad

    /*
    // Current key:
    property int currentKey: -1

    // Theo par:
    property int theoPar: 0

    // Show keypad:
    function onShowKeyPad(theoPar, actualPar)
    {
        // Reset current key:
        currentKey = -1

        // Set theopar:
        _numericKeyPad.theoPar = theoPar

        // Set current key:
        _numericKeyPad.currentKey = actualPar

        // Show keyboard:
        state = "on"
    }
    */
}
